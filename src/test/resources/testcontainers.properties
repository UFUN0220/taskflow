# Project-local Testcontainers configuration.
# Keep Docker client selection independent from the user's profile directory.
docker.client.strategy=org.testcontainers.dockerclient.EnvironmentAndSystemPropertyClientProviderStrategy
